# Server dependencies for web dashboard
# Used by Render.com / Railway / Fly.io deployments
# NOTE: Render free tier has 512MB RAM — keep this list lean!

# Web framework
flask
gunicorn

# Content fetching (used by scheduler + data extractor)
requests
beautifulsoup4
lxml
dateparser

# YouTube (lazy-loaded, only for manual "Get YouTube News" feature)
youtube-transcript-api
scrapetube
# yt-dlp          # DISABLED — ~80MB in memory, not needed for server scheduler
                   # Re-enable if YouTube audio download is needed on server

# AI summarization (lazy-loaded, only for YouTube + newsletter features)
google-generativeai

# Google Sheets (used by scheduler for export + config persistence)
google-api-python-client
google-auth

# Audio — Fast mode (lightweight, works within 512MB)
gTTS

# Security
Flask-Limiter          # Rate limiting for API endpoints

# Utilities
python-dotenv
