# Desktop GUI Application Requirements
# Install with: pip install -r requirements-desktop.txt

# GUI Framework
customtkinter>=5.2.0
tkcalendar>=1.6.1
pillow>=9.0.0
qrcode>=7.3

# Audio Processing
numpy>=1.21.0
soundfile>=0.12.0
kokoro-onnx>=0.4.0

# Web & Data Processing
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
python-dotenv>=1.0.0
dateparser>=1.1.0

# YouTube & Media
youtube-transcript-api>=0.6.0
scrapetube>=2.5.0
# 2026-08-03: floor raised from 2023.1.0. The old pin resolved to 2025.11.12,
# which 403'd on every subtitle download (72 in one run) — videos without a
# transcript get dropped, silently shrinking the briefing. 2026.7.4 fetches
# them cleanly. YouTube moves fast; keep this current.
yt-dlp>=2026.7.4

# Google APIs
google-generativeai>=0.3.0

# LLM Fallback (Groq free tier — optional, used when Gemini rate-limited)
groq>=0.4.0
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0

# Text-to-Speech (optional - for fast TTS)
gTTS>=2.3.0

# Transcription (optional - for whisper transcription)
# faster-whisper>=0.9.0  # Uncomment if you want transcription support

# Build tools (for packaging)
# pyinstaller>=5.0  # Install separately for building

# Agent interface (MCP)
# Powers `dab-mcp` / mcp_server.py — the stdio MCP server that lets Claude Code,
# Claude Desktop, Cowork, Cursor etc. start audio jobs with the GUI closed.
mcp>=1.0,<2  # 2.x renamed FastMCP -> MCPServer; mcp_server.py targets the 1.x API
